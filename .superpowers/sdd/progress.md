# Subagent Driven Development Progress

- Baseline: swift test passed (5 tests, 0 failures) on 2026-07-11 12:36 local.
- Workspace: original folder, not a Git repository; using checkpoint reports instead of commits.
- Preflight: adjusted Task 9 to avoid depending on Task 10 extractor; adjusted Task 12 to assert v2 magic.
- Task 1: verified with `swift test --filter PackageBoundaryTests` (2 tests, 0 failures) and `swift test` (7 tests, 0 failures); review fixes applied.
- Task 1 verified with swift test --filter PackageBoundaryTests
- Task 2: fix verification passed with `swift test --filter PublicAPIBoundaryTests`, `swift test --filter ZwzV2TypesTests`, `swift test --filter PackageBoundaryTests`, and `swift test` (10 tests, 0 failures).
- Task 2 verified with swift test --filter ZwzV2TypesTests
- Task 3: final verification passed with `swift test --filter ZwzV2BinaryCodecTests` (16 tests, 0 failures) and `swift test` (26 tests, 0 failures); review clean after encoder/decoder overflow fixes.
- Task 3 verified with swift test --filter ZwzV2BinaryCodecTests
- Task 4: final verification passed with `swift test --filter ZwzV2PathTests` (6 tests, 0 failures) and `swift test` (32 tests, 0 failures); review clean after file/descendant conflict fix.
- Task 4 verified with swift test --filter ZwzV2PathTests
- Task 5: final verification passed with `swift test --filter ZwzV2BlockCodecTests` (6 tests, 0 failures) and `swift test` (38 tests, 0 failures); review clean after Deflate threshold test seam.
- Task 5 verified with swift test --filter ZwzV2BlockCodecTests
- Task 6: verification passed with `swift test --filter ZwzV2CryptoTests` (2 tests, 0 failures) and `swift test` (40 tests, 0 failures); review approved with minor future hardening suggestions for nonce byte-layout and tamper tests.
- Task 6 verified with swift test --filter ZwzV2CryptoTests
- Task 7: final verification passed with `swift test --filter ZwzV2IndexCodecTests` (10 tests, 0 failures), `swift test --filter ZwzV2CryptoTests` (2 tests, 0 failures), and `swift test` (50 tests, 0 failures); review clean after timestamp/privacy/tamper fixes.
- Task 7 verified with swift test --filter ZwzV2IndexCodecTests
- Task 8: final verification passed with `swift test --filter ZwzV2VolumeIOTests` (10 tests), `swift test --filter ZwzV2BinaryCodecTests` (16 tests), and `swift test` (60 tests); review clean after zero-based/reordered-volume fixes.
- Task 8 verified with swift test --filter ZwzV2VolumeIOTests
- Task 9: final verification passed with `swift test --filter ZwzV2CompressorTests` (4 tests) and `swift test` (64 tests); follow-up review approved after bounded reorder-window backpressure and split/block-boundary tests. Index materialization remains a format/API follow-up, not a Task 9 blocker.
- Task 9 verified with swift test --filter ZwzV2CompressorTests
- Task 10: final verification passed with `swift test --filter ZwzV2ExtractorTests` (7 tests) and `swift test` (71 tests); review approved after symlink destination rejection, index layout validation, and existing-output truncation fixes.
- Task 10 verified with swift test --filter ZwzV2ExtractorTests

## ZWZ V3 Public-Key Encryption (2026-07-12)

- Baseline: `swift test` passed 178 tests, 1 skipped, 0 failures before V3 implementation.
- Preflight decision: preserve public V1 and V2 behavior; add independent V3.
- V3 Task 1: complete (commits `6ba4f7e..7d5079a`, review approved). Focused types tests 5/5 passed; compatibility follow-up `ZwzV2APITests` 5/5 and `ZwzHiddenFilePreviewTests` 2/2 passed. Minor review note for broader public-contract tests deferred to final review ledger.
- V3 Task 2: complete (commits `7d5079a..c2636b0`, review approved after fingerprint domain-separation fix). Crypto tests 7/7 passed; full suite passed on the implementation commit.
- V3 Task 3: complete (commits `d6514a9`, `8469822`; implementation and rereview approved). Defined the canonical 160-byte header, absolute LE region layout, recipient/signer records, canonical signature bytes, checked parsing, and malicious recipient-count bounds. `ZwzV3BinaryCodecTests` passed 10/10. The initial review's two Minor test gaps were closed with an independent golden header vector and direct boundary mutations. Residual Task 4 consideration: avoid multiple whole-archive `Data` copies for very large valid archives.
- V3 Task 4: complete (commits `a32a7ea`, `ce77625`; final rereview approved). Added authenticated block/index workflows, multi-recipient unlock, pre-decryption signature verification, full/single/subtree extraction, cancellation cleanup, atomic replacement, and split volumes. Initial review found a Critical known-signer spoof plus Important integer-overflow and unbounded whole-archive-read issues; fixes bind trust to fingerprint+actual signing public key, use checked arithmetic, and impose a pre-read 512 MiB logical archive budget with cancellable chunked split reads. `swift test --filter ZwzV3` passed 44/44; full `swift test` executed 222 tests with 1 skipped fixture and 0 failures in about 232 seconds. Residual non-blocker: parser copies can make peak memory exceed the logical-size budget near the limit.
- V3 Task 5: complete (commits `674e31d`, `6715d62`; final rereview approved). Added canonical `ZWZP` public files, scrypt N=65536/r=8/p=1 + AES-GCM `ZWZB` private backups, in-memory identity storage, and `.userPresence` Keychain storage. Initial review found a Critical destructive replace path plus Important concurrency and partial-delete issues; fixes use a shared injectable production transaction path, serialize Keychain operations while keeping scrypt outside the lock, preserve existing private keys on same-fingerprint replace, commit contact upgrades before deleting contacts, and attempt all delete items. Codec passed 7/7, Store passed 11/11, existing ZwzV3 regression passed 44/44. Real signed-app Keychain prompts/password fallback remain a manual integration check.
- V3 Task 6: complete (commits `8960d7f`, `035413c`; final rereview approved). Routed public-key compression/list/extract/single-entry through existing ZwzCore APIs with structured security results and source-compatible legacy shapes. Dispatch reads logical magic for shared V2/V3 split envelopes; V2 metadata comes from actual header flags and V3 errors remain concrete. Initial review found Important bare-`ZWZ3` detection and `z100+` discovery gaps; fixes support renamed archives and arbitrary-width volume numbers with >100-volume V2/V3 public-API tests. Task6 passed 14/14, V2 API 5/5, focused preview/cleanup had 30 pass and 1 skipped fixture. Existing user cleanup diffs remain unstaged. V1 remains explicitly unsupported because this repository has no historical implementation/fixture.
- V3 Task 7: complete (commits `74d6d7b`, `7b140e8`; final rereview approved). Added testable CLI parser/runner, eight key lifecycle commands, repeatable recipients, local signing, stdin/TTY backup passwords, atomic key-file output, recipient recovery guidance, concrete security errors, and signature status. Initial review found an Important help regression that hid legacy options and public-key prerequisites; the fix restored full legacy/new workflow documentation and added split/missing/truncated recipient-info tests. CLI passed 16/16, recipient info 4/4, V3 API 14/14, V2 API 5/5, and actual help exited 0. Real TTY and signed-app Keychain prompts remain manual checks. The Task 7 pause was lifted when the user resumed development.
- V3 Task 8: complete and paused per user request (commit `7d51a20`; final rereview approved). Added a settings key-management page with separate local identities and contacts, two-phase deletion, public import/export, encrypted private backup/restore, explicit conflict replacement, one-shot archive-resume callback, 16 MiB owned key-file reads, atomic output, and off-main Keychain/scrypt/file work. Initial review found three Important issues: post-mutation listing failure was misreported as mutation failure, submitted synchronous work exposed a false cancel path, and overlapping file reads could mismatch conflict data. Fixes split mutation commit from refresh, preserve optimistic local state and the one-shot callback, synchronously lock submissions, add file-operation generations, and bind fingerprint plus data atomically. IdentityManager passed 19/19, existing settings passed 1/1, and the ZwzGUI target built successfully. Review package: `.superpowers/sdd/v3-task-8-review.diff`. Signed-app Touch ID/login-password fallback and cancellation remain manual checks. Do not start Task 9 until the user resumes.
- V3 Task 9: complete and paused before Task 10 (commit `1ccdd9b`; final rereview approved). Added one application-owned GUI identity store, injectable archive/edit/mount clients, V3 public inspection before private-key lookup, three-mode compression with deterministic recipients and local-only signing, structured signature badges, one-shot missing-key restore recovery, invalid-signature enforcement at every content boundary, and public-key preservation for editing and virtual-disk rebuilds. Review found five Important issues in session-owned protection, signer public-key binding, persisted passwords, and stale editor callbacks; fixes made edit/mount sessions authoritative, bound signer fingerprint plus Ed25519 public key, removed passwords from persisted sessions, and added generation checks. Rereview found two V2 migration issues; fixes prompt for and verify an original password after restart and migrate only the non-sensitive `.password` protection marker from legacy JSON, preventing silent downgrade. Core inspection passed 8/8, workflow tests 20/20, all ZwzGUI tests 131/131, V2/V3 API regressions 19/19, and `swift build --target ZwzGUI` passed. Review package: `.superpowers/sdd/v3-task-9-review.diff`. Signed-app Keychain/password dialogs and real `hdiutil` behavior remain manual integration checks.
- V3 Task 10: complete; the V3 public-key project is fully implemented (commit `42a1ff7`; final rereview approved with no findings). Added committed SHA-256-locked V1 detection-only, V2 unencrypted/password, and V3 unsigned/signed multi-recipient fixtures; compatibility tests cover detect/inspect/list/extract, both V3 recipients, known/unknown signatures, wrong V2 password, signed canonical-byte mutation, and authenticated ciphertext mutation. Fixtures are SwiftPM test resources and all embedded keys/passwords are conspicuously test-only. README now documents exact GUI/Core/CLI workflows, public untrusted labels, mode exclusivity, Keychain user presence, backup/lost-key consequences, V2 compatibility, and truthful V1 unsupported behavior. Bundle validation now checks structure, resources, owner/group/other readability/traversability/executability, pre-sign executable validity, and strict post-sign bundle validity. Initial final review found two Important issues (incomplete Unix permission validation and unhandled source fixtures); both were fixed and independently verified with positive/negative bundle tests. Final `swift test` executed 346 tests with 1 existing RAR-fixture skip and 0 failures in 440.5s. App/DMG and Installer PKG builds succeeded; review package: `.superpowers/sdd/v3-task-10-review.diff`. Remaining release limitations: PKG was built but not installed, artifacts are ad-hoc signed and not notarized, and real Touch ID/login-password plus `hdiutil` interaction require signed-app manual testing.
